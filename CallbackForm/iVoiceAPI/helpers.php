<?php


    /* $old = array();
    if(isset($_GET['old'])) {
        $old = json_decode(base64_decode(urldecode($_GET['old'])), true);
    }

    $settings = include_once(__DIR__.'/settings.ini.php');

    function old($key, $default = '') {
        global $old;

        return (isset($old[$key])) ? $old[$key] : $default;
    }

    function get_settings($key) {
        global $settings;

        return (isset($settings[$key])) ? $settings[$key] : false;
    } */
?>